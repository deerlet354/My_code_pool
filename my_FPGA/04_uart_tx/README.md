This project implements the TX transmission logic for the UART serial port, utilizing a state machine and the serial port data format, which includes one start bit, eight data bits, and one stop bit. The baud rate is 115200. This project is for PL UART and does not rely on the ARM processor. The pins are located on the PL side and must not be mistakenly assigned to the MIO pins on the PS side...